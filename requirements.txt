# --- Runtime (Phase 0) ---
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.10
pydantic-settings>=2.7

# --- Persistence (Phase 2) ---
sqlalchemy>=2.0
alembic>=1.13
# Prod driver (Postgres). Dev/test default to the stdlib sqlite driver.
psycopg[binary]>=3.2

# --- Grading (Phase 5) ---
rapidfuzz>=3.9       # fuzzy match for gap_fill acceptable misspellings
anthropic>=0.40      # real LLMGrader (open_writing); mocked in tests

# --- Dev / tooling ---
pytest>=8.3
httpx>=0.28          # FastAPI TestClient transport
ruff>=0.9
mypy>=1.14
pre-commit>=4.0
